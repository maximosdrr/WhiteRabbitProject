import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:white_rabbit_app/app/modules/registration/registration_page.dart';

void main() {
  testWidgets('RegistrationPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(RegistrationPage(title: 'Registration')));
    //  final titleFinder = find.text('Registration');
    //  expect(titleFinder, findsOneWidget);
  });
}
